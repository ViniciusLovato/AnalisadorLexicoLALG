{ Testes com declaracao de variaveis }
program prog;
const minhavar1 = 10.1;
var minhaVar2 : real;
var minharVar3 : integer;
begin
	while(1 > 2) do
	begin
		write(minhavar);
	end;
	write(vari)
	read(vari);
end.
